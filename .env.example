# Copy this file to .env for local development (never commit .env)
PORT=3000
JWT_SECRET=change-this-to-a-long-random-string

# Railway: set RAILWAY_VOLUME_MOUNT_PATH to /data in your Railway service
# to persist the SQLite database across deploys.
